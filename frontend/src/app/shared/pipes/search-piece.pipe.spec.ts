import { SearchPiecePipe } from './search-piece.pipe';

describe('SearchPiecePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPiecePipe();
    expect(pipe).toBeTruthy();
  });
});
